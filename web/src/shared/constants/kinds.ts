/** NIP-AP agent persona event (mirror of buzz-core's KIND_PERSONA). */
export const KIND_PERSONA = 30175;

/** NIP-AP managed-agent event (mirror of buzz-core's KIND_MANAGED_AGENT). */
export const KIND_MANAGED_AGENT = 30177;
